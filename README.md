This project is about adding recipes to a main repository and pushing code to GitHub.

The first steps were to add three recipes and include anchor tags that link to these 3 recipes.
Each recipe contains a title of the recipe with an image and a short description.
Within each recipe there is also a list of steps and ingredients.